<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <script>
            $("#idDeTonSelect").change(function (){
                var val = $(this).val();
                if (val == "valeur1"){
                    // redirection via JS
                    document.location.replace("une-url");
                }else if (val == "valeur2"){
                    // autre action...
                }
            });
        </script>
        <iframe src="test.pdf" width="600" height="800" align="middle"></iframe>
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>



</html>
